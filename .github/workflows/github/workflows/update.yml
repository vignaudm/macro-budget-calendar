name: Update Macro-Budget Calendar
on:
schedule:
- cron: "0 */3 * * 1-5"
workflow_dispatch:
push:
branches: ["main"]
jobs:
run:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v4
- uses: actions/setup-python@v5
with:
python-version: "3.11"
- run: pip install -r requirements.txt
- name: Write service account key
run: echo "secrets.GCP 
S
​
 A 
J
​
 SON">sa.json−name:Runupdaterenv:CALENDAR 
I
​
 D:{{ secrets.CALENDAR_ID }}
GOOGLE_APPLICATION_CREDENTIALS: sa.json
run: python updater.py
